# Supply chain security hardening
# Blocks postinstall script execution (primary vector for npm supply chain attacks)
# After npm ci, run `npm rebuild` only if you need native addon compilation
ignore-scripts=true
package-lock=true
audit=true
